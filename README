From the bottomless pit 
of a Mirrored heart,
May this untethered workflow
give you advantages from the start.


make your account on git, setup your git environment, with the username and useremail
clone the repo you want with git clone <https:>

to add stuff to git repo
On git, make a new repo after you have your account all setup 
get the internet adddress of it https://...
git init
git add .
git commit -m "banana"
git remote add origin <https>
git push -u origin master //or origin main 

//next time you push, no need for the flag.